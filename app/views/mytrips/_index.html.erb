<h1>Listing trips</h1>

<!--<table>
<thead>
  <tr>
    <th>From</th>
    <th>To</th>
    <th>Via</th>
    <th>Trip Date</th>
    <th>Start Time</th>
    <th>Max Riders</th>
    <th>Comments</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
</thead>

 <tbody> -->
<% @trips.each do |trip| %>
  <ul data-role = "listview" data-theme = "a" data-inline = "true">
   <!-- <td><%= trip.from %></td>
    <td><%= trip.to %></td>
    <td><%= trip.via %></td>
    <td><%= trip.tripdate %></td>
    <td><%= trip.starttime %></td>
    <td><%= trip.maxriders %></td>
    <td><%= trip.comments %></td> -->
    <li><%= link_to trip.from + '-' + trip.to + 'via' + trip.via + 'on' + trip.tripdate.to_s + '|' + trip.starttime.to_s, trip %></li>
   <!-- <td><%= link_to 'Edit', edit_trip_path(trip) %></td>
    <td><%= link_to 'Destroy', trip, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
  </tr>
<% end %>
</tbody>
</table>

<br />

<%= link_to 'New Trip', new_trip_path %>
